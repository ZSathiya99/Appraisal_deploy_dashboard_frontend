    <div className="container-1 bg-blue-200 h-[130px] rounded-3xl w-[250px] flex items relative">
            <h1 className="absolute top-4 left-[50%] translate-x-[-50%] w-[90%] text-gray-900 text-sm ">Total Employees Form Submitted</h1>
            <ResponsiveContainer width="100%" height="100%">
              <PieChart>
                <Pie
                  data={data}
                  startAngle={180}
                  endAngle={0}
                  cx="50%"
                  cy="100%"
                  innerRadius={50}
                  outerRadius={70}
                  dataKey="value"
                  stroke="none"
                >
                  {data.map((entry, index) => (
                    <Cell key={`cell-${index}`} fill={COLORS[index]} />
                  ))}
                </Pie>
              </PieChart>
            </ResponsiveContainer>
            <h1 className="absolute bottom-3 left-[50%] translate-x-[-50%] font-semibold">
              86%
            </h1>
          </div>


          to={`/appraisal_form/form/completed`}